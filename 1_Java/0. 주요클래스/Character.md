# Character class

        -java.lang.Object
    	    -java.lang.Character

## - Character 클래스란?

- Character 클래스는 기본형 중 char의 값을 객체로 포장한다.
- Character 형태의 객체에는 char의 단일 필드가 들어간다.
- 이 클래스는 문자의 형태를 판별해서 문자를 대문자로부터 소문자(또는 그 반대)로 변환하기 위한 각종 매서드도 제공한다.
